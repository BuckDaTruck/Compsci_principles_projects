StringList names= new StringList("buckley", "johny");
int x = names.size();
int z
int a=int(random(5));
void setup() {

  System.out.println("this is "+squares(a));
  for (int i = 0; i < x; i++) {
    System.out.println(hello("Hi, "));
  }
}
void draw() {
  int a=int(random(5));
  //System.out.println("this is "+squares(a));
}
// this function will
String hello(String st) {

  String it = names.get(z);
  //String done = st+it;
   String done = st+it;
  return done;

}
void aplus1() {
  a=a+1;
}
int squares(int s) {
  int sq = s*s;
  return sq;
}
